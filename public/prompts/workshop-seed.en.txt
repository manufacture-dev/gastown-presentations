Use the agent-browser skill at http://localhost:5173 to create 12 fictional contracts through the interface.

First inspect the fields, options and existing contracts.
Vary available types, counterparties, amounts and currencies; past, present and future dates; and permitted statuses and optional fields.
Use only the app’s current capabilities. Do not force an unavailable status.

Do not change code or existing data. Do not use SQL or direct API calls. Avoid duplicates, even after an interruption.

Verify that each contract is saved. Summarize the 12 contracts and cases covered. If blocked, report what was created and what remains, without bypassing validation.
